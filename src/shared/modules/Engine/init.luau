return {
  Game = require(script.Game),
  Workspace = require(script.Workspace),

  Classes = {
    Character = require(script.Classes.Character),
  },

  GetGameObject = require(script.Parent.GetGameObject),
}